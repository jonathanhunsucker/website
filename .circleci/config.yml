version: 2
jobs:
  build:
    docker:
      - image: felicianotech/docker-hugo:latest
    steps:
      - checkout
      - run:
          name: Build
          command: script/deploy/build
      - run:
          name: Ship
          command: script/deploy/ship
workflows:
  version: 2
  deploy:
    jobs:
      - build
